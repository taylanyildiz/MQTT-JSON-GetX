import 'package:flutter/material.dart';

class PublisScreen extends StatelessWidget {
  const PublisScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
