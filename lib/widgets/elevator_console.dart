import 'package:flutter/material.dart';

class ElevatorConsole extends StatelessWidget {
  const ElevatorConsole({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
